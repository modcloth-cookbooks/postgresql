standby_mode                         = 'on'
primary_conninfo                     = 'host=<%= node[:postgresql][:replication_master_hostname] %> port=5432 user=replicant password=dabryifdel'
trigger_file                         = '/var/pgsql/pgsql.trigger'
restore_command                      = 'cp -f /var/pgsql/wals/%f %p < /dev/null'
archive_cleanup_command              = 'pg_archivecleanup /var/pgsql/wals/ %r'

